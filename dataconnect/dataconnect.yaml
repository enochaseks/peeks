specVersion: "v1"
serviceId: "peeks"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "peeks-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
